# To use builtin modules we have to import modules
import random           # importing the module 'random'
r_number = random.randint(0, 15)    # it generates the random number between 0 and 15
# print(r_number)
rand = random.random()*100          # it will generate the random numbers between 0 & 1
# print(rand)
lst = ["star plus", "dd1", "aj tak", "vaidu"]
choice = random.choice(lst)         # it will give any random choice from list
# print(choice)
# To use external modules we have to install modules
